# sqlite_db.py
# Week 5 - SQLite integration. Not started yet.
# Should follow the same pattern as redis_db.py, plus schema design,
# transactions, and concurrency handling (the 3 key features for this week).


class SQLiteManager:
    def __init__(self, *args, **kwargs):
        raise NotImplementedError("SQLite integration is coming in Week 5.")
